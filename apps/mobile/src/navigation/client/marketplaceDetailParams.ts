/** Shared by every client tab stack that can push MarketplaceDetail (Home/Search/Favorites) — see docs/PRODUCT.md "Client navigation". */
export type MarketplaceDetailParamList = {
  MarketplaceDetail: { publicationId: string };
};
