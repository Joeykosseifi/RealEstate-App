# Database

- **Engine:** PostgreSQL 16
- **ORM:** Prisma (`prisma/schema.prisma`)
- **Migrations:** Prisma Migrate — every schema change is a checked-in
  migration under `prisma/migrations/`. Never mutate the database outside
  of a migration.

## Current state (Milestone 0)

The schema currently defines only the `datasource` and `generator` blocks.
No domain models exist yet — this is intentional. Milestone 0 is
infrastructure only (connectivity, tooling, validation); business models
are introduced starting with Milestone 1.

## Planned model introduction, by milestone

This list tracks *when* each area of the schema is expected to land, not
its final field-level design (that is decided at the start of the
milestone that needs it, against the then-current schema).

| Milestone | Models introduced |
|---|---|
| 1 | `User`, email/phone verification records, refresh token/session records |
| 2 | `Workspace`, `WorkspaceMembership`, roles/permissions |
| 3 | `Property`, property media, owner info, private notes, location |
| 4 | CRM `Client`, `ClientRequirement`, match results, `Presentation` |
| 5 | Publication review/moderation records |
| 6 | Client favorites, public marketplace read models |
| 7 | `Conversation`, `Message`, `Viewing` |
| 8 | Company employee invitations, team roles |
| 9 | `Collaboration`, collaboration permissions, commission agreements |
| 10 | `Subscription`, `Plan`, plan usage/limits |
| 11 | `AuditLog` (may land earlier if a milestone needs it sooner) |

## Conventions (apply from Milestone 1 onward)

- Primary keys: UUID (v4), generated by the database (`gen_random_uuid()`
  via `pgcrypto`, or Prisma's `@default(uuid())` — decided when the first
  model is added).
- Every table gets `createdAt` / `updatedAt` timestamps.
- Soft-delete vs. hard-delete is decided per-entity based on the audit and
  historical-data requirements in the product spec (e.g. sold properties
  must remain queryable as history).
- Foreign keys are always indexed.
- No ORM entity is ever returned directly from an API response — every
  endpoint maps to an explicit DTO (see `docs/API.md`).

## Local setup

```bash
cp .env.example .env
npm run docker:up            # starts Postgres + Redis
npm run prisma:generate
npm run prisma:migrate       # applies/creates migrations (none yet)
```
